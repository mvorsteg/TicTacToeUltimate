package ttt;

/*Author Matthew Vorsteg
 * This enum was originally intended to represent the symbol of each cell,
 * but I did not end up using it in the final version of the game
 * It was left here to keep all methods of setCell() and setBlock() compatible
 */

public enum Design {
	X, O, TRI, SQUARE, HEX, NULL, STAR, EYE, GHOST, HAT, PIZZA, PIE, CROWN, DB, MR
}
